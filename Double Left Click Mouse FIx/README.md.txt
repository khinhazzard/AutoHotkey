# Double Left Click Mouse Fix

AutoHotkey script that helps prevent accidental double-clicks when a mouse registers two clicks from a single physical click.

## File

- `doubleclick_delay_v2.ahk` — Filters accidental duplicate left-clicks and preserves normal mouse click behavior.